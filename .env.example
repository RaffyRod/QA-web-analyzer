# QA Web Analyzer - Environment Variables
# Copy this file to .env and configure as needed
# Location: qa-web-analyzer/.env (root of the project)

# ============================================
# Backend Configuration
# ============================================

# Node.js environment (development/production)
# Default: development
NODE_ENV=development

# Backend server port (optional - will auto-detect available port if not set)
# Priority order: 3002 → 3003 → 3004 → 3005 → 3000 → 3001 → any available
# Default: auto-detected
# PORT=3002

# ============================================
# Frontend Configuration
# ============================================

# Vite dev server port (optional - will find alternative if occupied)
# Default: 5173
# VITE_PORT=5173

# Backend API URL (optional - will auto-detect backend port if not set)
# Only set this if you need to override the automatic detection
# Default: auto-detected from backend
# VITE_API_URL=http://localhost:3002

# ============================================
# Playwright Configuration
# ============================================

# Browser to use for analysis (chromium/firefox/webkit)
# Default: chromium
# PLAYWRIGHT_BROWSER=chromium

# Run browser in headless mode (true/false)
# Default: true
# PLAYWRIGHT_HEADLESS=true

# ============================================
# Analysis Configuration
# ============================================

# Analysis timeout in milliseconds
# Default: 30000 (30 seconds)
# ANALYSIS_TIMEOUT=30000
